# Generated by zipx. Do not edit. Run 'sbt zipxWorkflowGenerate' to regenerate.
name: CI
"on":
  push:
    branches:
      - main
  pull_request: null
  workflow_dispatch: null
concurrency:
  group: CI-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') && github.ref != 'refs/heads/main' }}
jobs:
  verify-gate:
    name: verify-gate
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && !startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: read
      pull-requests: read
    env:
      GITHUB_TOKEN: ${{ github.token }}
    outputs:
      run: ${{ steps.check.outputs.run }}
    steps:
      - name: Skip Verify after merged PR push
        id: check
        run: |
          # Commits landed by merging/squashing a PR are associated with that PR via the API.
          # The commit-to-PR index can lag after squash; retry rather than fail-open into a second Verify.
          prs=0
          for attempt in 1 2 3 4 5 6; do
            if [ "$prs" -eq 0 ]; then
              if [ "$attempt" -eq 2 ]; then
                sleep 1
              elif [ "$attempt" -eq 3 ]; then
                sleep 2
              elif [ "$attempt" -eq 4 ]; then
                sleep 4
              elif [ "$attempt" -eq 5 ]; then
                sleep 8
              elif [ "$attempt" -eq 6 ]; then
                sleep 16
              fi
              prs=$(gh api "repos/${{ github.repository }}/commits/${{ github.sha }}/pulls" \
                --jq "[.[] | select(.merged_at != null and .base.ref == \"${{ github.ref_name }}\")] | length")
            fi
          done
          if [ "$prs" -gt 0 ]; then
            echo "Merged PR push, skipping redundant Verify (already ran on the PR)"
            echo "run=false" >> "$GITHUB_OUTPUT"
          else
            echo "run=true" >> "$GITHUB_OUTPUT"
          fi
        env:
          GH_TOKEN: ${{ github.token }}
  cache-rehydrate:
    name: cache-rehydrate
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: needs.verify-gate.result == 'success' && needs.verify-gate.outputs.run == 'false'
    env:
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: cache-rehydrate
          node-version: ""
          sbt-disk-cache: "false"
          cache-mode: save
          cache-epoch: fd8132a86500a53a
      - name: cache-rehydrate
        run: sbt 'Test/compile'
  affected:
    name: affected
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ github.token }}
    outputs:
      modules: ${{ steps.compute.outputs.modules }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: affected
          node-version: ""
          sbt-disk-cache: "false"
          cache-mode: "off"
          cache-epoch: fd8132a86500a53a
      - name: Compute affected modules
        id: compute
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            BASE="${{ github.event.pull_request.base.sha }}"
            sbt -batch --error "zipxAffectedModules $BASE"
            modules=$(cat target/zipx-affected.json)
          elif [ "${{ github.event_name }}" = "push" ]; then
            before="${{ github.event.before }}"
            if [ -z "$before" ] || [ "$before" = "0000000000000000000000000000000000000000" ]; then
              modules='["all"]'
            else
              BASE="$before"
              sbt -batch --error "zipxAffectedModules $BASE"
              modules=$(cat target/zipx-affected.json)
            fi
          else
            modules='["all"]'
          fi
          echo "modules=$modules" >> "$GITHUB_OUTPUT"
  modver:
    name: modver
    runs-on: ubuntu-latest
    if: ((github.event_name == 'push') && ((github.ref == 'refs/heads/main'))) || (github.event_name == 'workflow_dispatch')
    permissions:
      contents: read
    env:
      GITHUB_TOKEN: ${{ github.token }}
    outputs:
      modules: ${{ steps.compute.outputs.modules }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: modver
          node-version: ""
          sbt-disk-cache: "false"
          cache-mode: "off"
          cache-epoch: fd8132a86500a53a
      - name: Compute version-moved modules
        id: compute
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            sbt -batch --error zipxModverPublishModules
            modules=$(cat target/zipx-modver-modules.json)
          elif [ "${{ github.event_name }}" = "push" ]; then
            before="${{ github.event.before }}"
            if [ -z "$before" ] || [ "$before" = "0000000000000000000000000000000000000000" ]; then
              echo "zipx: github.event.before is missing or all-zero; refusing to guess the publish set"
              exit 1
            else
              sbt -batch --error "zipxModverPublishModules $before"
              modules=$(cat target/zipx-modver-modules.json)
            fi
          else
            echo "zipx: github.event.before is missing or all-zero; refusing to guess the publish set"
            exit 1
          fi
          echo "modules=$modules" >> "$GITHUB_OUTPUT"
  test:
    name: test
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    env:
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: test
          node-version: ""
          sbt-disk-cache: "false"
          cache-mode: save
          cache-epoch: fd8132a86500a53a
      - name: test
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; testFull'
          else
            sbt 'testFull'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  fmt:
    name: fmt
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    env:
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: fmt
          node-version: ""
          sbt-disk-cache: "false"
          cache-mode: restore
          cache-epoch: fd8132a86500a53a
      - name: fmt
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; scalafmtCheckAll'
          else
            sbt 'scalafmtCheckAll'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  workflow-check:
    name: workflow-check
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    env:
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: workflow-check
          node-version: ""
          sbt-disk-cache: "false"
          cache-mode: restore
          cache-epoch: fd8132a86500a53a
      - name: workflow-check
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; zipxWorkflowCheck'
          else
            sbt 'zipxWorkflowCheck'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  advisories:
    name: advisories
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    env:
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: advisories
          node-version: ""
          sbt-disk-cache: "false"
          cache-mode: restore
          cache-epoch: fd8132a86500a53a
      - name: advisories
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; zipxAdvisoryCheck'
          else
            sbt 'zipxAdvisoryCheck'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  modver-suggest:
    name: modver-suggest
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: (!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))) && (github.event_name == 'pull_request')
    permissions:
      contents: read
      pull-requests: write
    env:
      GITHUB_TOKEN: ${{ github.token }}
      ZIPX_MODVER_BASE_SHA: ${{ github.event.pull_request.base.sha }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: modver-suggest
          node-version: ""
          sbt-disk-cache: "false"
          cache-mode: restore
          cache-epoch: fd8132a86500a53a
      - name: Fetch PR base SHA
        run: git fetch --no-tags origin "$ZIPX_MODVER_BASE_SHA"
      - name: modver-suggest
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; zipxModverSuggest'
          else
            sbt 'zipxModverSuggest'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  modver-check:
    name: modver-check
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: (!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))) && (github.event_name == 'pull_request')
    permissions:
      contents: read
    env:
      GITHUB_TOKEN: ${{ github.token }}
      ZIPX_MODVER_BASE_SHA: ${{ github.event.pull_request.base.sha }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: modver-check
          node-version: ""
          sbt-disk-cache: "false"
          cache-mode: restore
          cache-epoch: fd8132a86500a53a
      - name: Fetch PR base SHA
        run: git fetch --no-tags origin "$ZIPX_MODVER_BASE_SHA"
      - name: modver-check
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; zipxModverCheck'
          else
            sbt 'zipxModverCheck'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  image:
    name: image
    runs-on: ubuntu-latest
    needs:
      - affected
      - verify-gate
    if: (!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))) && (!cancelled() && needs.affected.outputs.modules != '[]')
    strategy:
      fail-fast: false
      matrix:
        module:
          - svcA
          - svcB
          - workerA
          - workerB
    env:
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - if: (contains(fromJson(needs.affected.outputs.modules), matrix.module) || contains(fromJson(needs.affected.outputs.modules), 'all'))
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        if: (contains(fromJson(needs.affected.outputs.modules), matrix.module) || contains(fromJson(needs.affected.outputs.modules), 'all'))
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: image
          node-version: ""
          sbt-disk-cache: "false"
          cache-mode: restore
          cache-epoch: fd8132a86500a53a
      - name: image
        if: (contains(fromJson(needs.affected.outputs.modules), matrix.module) || contains(fromJson(needs.affected.outputs.modules), 'all'))
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; ${{ matrix.module }}/Docker/publishLocal'
          else
            sbt '${{ matrix.module }}/Docker/publishLocal'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
      - name: Check staged jars are uninstrumented
        if: (contains(fromJson(needs.affected.outputs.modules), matrix.module) || contains(fromJson(needs.affected.outputs.modules), 'all'))
        run: |
          jars=$(find . -path '*/docker/stage/*' -name '*.jar')
          if [ -z "$jars" ]; then
            echo "no staged jars to check"
            exit 1
          fi
          for jar in $jars; do
            if unzip -p "$jar" | grep -ac coverage/Invoker >/dev/null 2>&1; then
              echo "instrumented classes in $jar"
              exit 1
            fi
          done
          echo "no coverage instrumentation in staged jars"
  image-it:
    name: image-it
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    env:
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: image-it
          node-version: ""
          sbt-disk-cache: "false"
          cache-mode: restore
          cache-epoch: fd8132a86500a53a
      - name: image-it
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; imageIt/testFull'
          else
            sbt 'imageIt/testFull'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  legacy:
    name: legacy
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    env:
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: legacy
          node-version: ""
          sbt-disk-cache: "false"
          cache-mode: restore
          cache-epoch: fd8132a86500a53a
      - name: legacy
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; ++2.13.18; legacy/testFull'
          else
            sbt '++2.13.18; legacy/testFull'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  publish-models:
    name: publish models
    runs-on: ubuntu-latest
    needs:
      - modver
    if: (!cancelled() && (((github.event_name == 'push') && ((github.ref == 'refs/heads/main'))) || (github.event_name == 'workflow_dispatch')) && contains(fromJson(needs.modver.outputs.modules), 'models')) && ((github.event_name == 'push') && (github.ref == 'refs/heads/main'))
    permissions:
      contents: read
      packages: write
    env:
      GITHUB_TOKEN: ${{ github.token }}
      ZIPX_MODVER_REGISTRY: ghpkg:early-effect/zipx-ci-lab
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: publish-models
          node-version: ""
          sbt-disk-cache: "false"
          cache-mode: restore
          cache-epoch: fd8132a86500a53a
      - name: publish
        run: sbt '+models/publish'
  publish-modelsJS:
    name: publish modelsJS
    runs-on: ubuntu-latest
    needs:
      - modver
    if: (!cancelled() && (((github.event_name == 'push') && ((github.ref == 'refs/heads/main'))) || (github.event_name == 'workflow_dispatch')) && contains(fromJson(needs.modver.outputs.modules), 'modelsJS')) && ((github.event_name == 'push') && (github.ref == 'refs/heads/main'))
    permissions:
      contents: read
      packages: write
    env:
      GITHUB_TOKEN: ${{ github.token }}
      ZIPX_MODVER_REGISTRY: ghpkg:early-effect/zipx-ci-lab
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: publish-modelsJS
          node-version: ""
          sbt-disk-cache: "false"
          cache-mode: restore
          cache-epoch: fd8132a86500a53a
      - name: publish
        run: sbt '+modelsJS/publish'
  publish-lib:
    name: publish lib
    runs-on: ubuntu-latest
    needs:
      - modver
      - publish-models
    if: (!cancelled() && (((github.event_name == 'push') && ((github.ref == 'refs/heads/main'))) || (github.event_name == 'workflow_dispatch')) && contains(fromJson(needs.modver.outputs.modules), 'lib') && needs.publish-models.result != 'failure') && ((github.event_name == 'push') && (github.ref == 'refs/heads/main'))
    permissions:
      contents: read
      packages: write
    env:
      GITHUB_TOKEN: ${{ github.token }}
      ZIPX_MODVER_REGISTRY: ghpkg:early-effect/zipx-ci-lab
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: publish-lib
          node-version: ""
          sbt-disk-cache: "false"
          cache-mode: restore
          cache-epoch: fd8132a86500a53a
      - name: publish
        run: sbt '+lib/publish'
  docker:
    name: docker
    runs-on: ubuntu-latest
    needs:
      - affected
    if: (!cancelled() && needs.affected.outputs.modules != '[]') && ((github.event_name == 'push') && (github.ref == 'refs/heads/main'))
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        module:
          - svcA
          - svcB
          - workerA
          - workerB
    env:
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - if: (contains(fromJson(needs.affected.outputs.modules), matrix.module) || contains(fromJson(needs.affected.outputs.modules), 'all'))
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        if: (contains(fromJson(needs.affected.outputs.modules), matrix.module) || contains(fromJson(needs.affected.outputs.modules), 'all'))
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: docker
          node-version: ""
          sbt-disk-cache: "false"
          cache-mode: restore
          cache-epoch: fd8132a86500a53a
      - name: Log in to GHCR
        if: (contains(fromJson(needs.affected.outputs.modules), matrix.module) || contains(fromJson(needs.affected.outputs.modules), 'all'))
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}
      - name: docker
        if: (contains(fromJson(needs.affected.outputs.modules), matrix.module) || contains(fromJson(needs.affected.outputs.modules), 'all'))
        run: sbt '${{ matrix.module }}/Docker/publish'
  registry-svcA:
    name: registry svcA
    runs-on: ubuntu-latest
    needs:
      - affected
      - docker
    if: (!cancelled() && (contains(fromJson(needs.affected.outputs.modules), 'svcA') || contains(fromJson(needs.affected.outputs.modules), 'all')) && needs.docker.result != 'failure') && ((github.event_name == 'push') && (github.ref == 'refs/heads/main'))
    permissions:
      contents: read
    env:
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: Register image
        run: echo registered "$LAB_IMAGE"
        env:
          LAB_IMAGE: ghcr.io/early-effect/zipx-ci-lab-svc-a:main-${{ github.sha }}
  registry-svcB:
    name: registry svcB
    runs-on: ubuntu-latest
    needs:
      - affected
      - docker
    if: (!cancelled() && (contains(fromJson(needs.affected.outputs.modules), 'svcB') || contains(fromJson(needs.affected.outputs.modules), 'all')) && needs.docker.result != 'failure') && ((github.event_name == 'push') && (github.ref == 'refs/heads/main'))
    permissions:
      contents: read
    env:
      GITHUB_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: Register image
        run: echo registered "$LAB_IMAGE"
        env:
          LAB_IMAGE: ghcr.io/early-effect/zipx-ci-lab-svc-b:main-${{ github.sha }}
  deploy-workers:
    name: deploy-workers
    runs-on: ubuntu-latest
    needs:
      - affected
      - docker
    if: (!cancelled() && needs.affected.outputs.modules != '[]' && needs.docker.result != 'failure') && ((github.event_name == 'push') && (github.ref == 'refs/heads/main'))
    environment: ${{ matrix.environment }}
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - module: workerA
            target: prd
            environment: lab-prd
            TIER: prd
          - module: workerA
            target: stg
            environment: lab-stg
            TIER: stg
          - module: workerB
            target: prd
            environment: lab-prd
            TIER: prd
          - module: workerB
            target: stg
            environment: lab-stg
            TIER: stg
    env:
      GITHUB_TOKEN: ${{ github.token }}
      TIER: ${{ matrix.TIER }}
    steps:
      - if: (contains(fromJson(needs.affected.outputs.modules), matrix.module) || contains(fromJson(needs.affected.outputs.modules), 'all'))
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        if: (contains(fromJson(needs.affected.outputs.modules), matrix.module) || contains(fromJson(needs.affected.outputs.modules), 'all'))
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "25"
          runner-os: ubuntu-latest
          cache-key-suffix: deploy-workers
          node-version: ""
          sbt-disk-cache: "false"
          cache-mode: restore
          cache-epoch: fd8132a86500a53a
      - name: deploy-workers
        if: (contains(fromJson(needs.affected.outputs.modules), matrix.module) || contains(fromJson(needs.affected.outputs.modules), 'all'))
        run: sbt '${{ matrix.module }}/promote'
